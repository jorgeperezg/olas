[tool.poetry]
name = "olas"
version = "0.1.0"
description = ""
authors = ["jorge.perez <j.perez@metocean.co.nz>"]

[tool.poetry.dependencies]
python = "^3.7"
xarray = "^0.17.0"
Cartopy = "^0.18.0"
dask = "^2021.3.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
